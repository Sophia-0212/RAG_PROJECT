## 15. 一致性检查清单

- 任何地方提到规模，都使用 18 workspace、640 用户、190 DAU、74 工作流。
- 任何地方提到知识库，都使用 126,400 文档、186 万 chunk。
- 任何地方提到流量，都使用 11,800 工作流运行/日、4,600 RAG 问答/日、峰值 9 QPS。
- 任何地方提到审批，都使用 730/日、6.2%、恢复成功率 99.93%。
- 提到仓库可核验评测资产时，使用 64 条 V1（development 16、locked 40、security 8）；提到 1,800 条锁定集和第 7.2 节表格时，必须明确是尚未落地的模拟目标。
- 任何地方提到线上结果，都使用 6 周稳定分桶和第 7.4 节表格。
- 不把正确率 80.1% 说成“准确率 80.1%”而不解释分母和 rubric。
- 不把 5 年说成 5 年现代 LLM Agent；准确说法是 5 年工程、3 年多 LLM/Agent。
- 不把 Self-RAG-inspired 工作流说成复现或训练了原论文模型。
- 不把模拟材料当作百度真实内部数据、事故或成果。
- 任何地方提到 RAG 生产状态，都使用 PostgreSQL 保存 checkpoint/会话，Redis 提供跨副本 lease 和全局租户配额；SQLite 只用于本地单进程。
- 任何地方提到部署基线，都使用 3 Pod×1 worker×12 并发、HPA 3-9、每 Pod PostgreSQL 池上限 16，并说明这些是待目标环境校准的初始值。
- 任何地方提到自动恢复，都使用 3 个 API Pod + 2 个恢复 Worker、请求级 checkpoint thread、PostgreSQL run lease/fencing 和 Redis 会话 lease；不再声称“有 checkpoint 就会自动恢复”。
- 不把 `invoke(None)` 说成 exactly-once；已提交 superstep 不重放，但失败节点可能再次执行，业务写操作仍需外部 execution_id。
- 不把新增离线测试和本地 PostgreSQL/Redis 集成用例说成目标集群 Pod-kill、IAM 联调或生产恢复率证据。
- 不把 RAG Redis lease 说成 ONECRM 业务幂等；业务副作用仍依赖 action hash、CAS 和 execution_id。
- 不把隔离容器集成测试、本机负载 smoke 或 Kubernetes YAML 说成生产 HA、容量、SLO 已验证。

---

本文件仅用于虚拟项目推演和面试训练。真正用于简历或面试前，必须将每一项替换为本人能够解释、复盘并承担责任的真实经历。
