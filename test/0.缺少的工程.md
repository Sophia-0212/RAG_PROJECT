----

1.由于不同用户有不同权限，查向量数据库的时候，先鉴权还是先召回？

----

明白。代码与文档的第一轮差距分析已经完成，暂未修改业务代码。

  当前结论

  现有项目属于“具备真实 RAG 算法链路的工程原型”，还不能算企业级 RAG Service：

  - 已有 Milvus Dense + BM25 混合检索、RRF 融合、CrossEncoder 重排。
  - Graph 2 已具备路由、检索、重排、文档评分、生成、幻觉检查和问题改写，适合作为后续主流程。
  - 但 11 项企业级能力中，6 项部分具备、5 项缺失，没有一项形成完整生产闭环。

  关键证据包括：

  - Milvus 建库会直接删除旧集合，无法增量更新和回滚：Desktop/workspace/RAG企业知识库项目/RAG_PROJECT/documents/milvus_db.py:81
  - 检索过滤只有 category，没有租户、用户和 ACL：Desktop/workspace/RAG企业知识库项目/RAG_PROJECT/tools/retriever_tools.py:6
  - Graph 状态只有少量字段，缺少请求身份、引用、版本、预算和失败原因：Desktop/workspace/RAG企业知识库项目/RAG_PROJECT/graph2/graph_state2.py:6
  - Graph 2 没有持久化检查点，部分改写链路缺少全局终止预算：Desktop/workspace/RAG企业知识库项目/RAG_PROJECT/graph2/graph_2.py:147
  - Agent 在模块导入时就执行真实调用：Desktop/workspace/RAG企业知识库项目/RAG_PROJECT/agent/rag_agent.py:44
  - 项目入口仍是 PyCharm 示例：Desktop/workspace/RAG企业知识库项目/RAG_PROJECT/main.py:1

  执行模块

  1. 工程基线与职责边界
     建立依赖锁定、配置管理、依赖注入、标准目录、README 和测试入口；消除导入时的外部调用；确定 Graph 2 为唯一生产主流程。

  2. 评测基线与发布门禁
     建立真实且版本化的测试集，覆盖召回、重排、引用、拒答、权限、安全、延迟和成本，并接入 CI。这个模块会与模块 1 同时启动。

  3. 企业级知识入库与索引治理
     增加稳定文档/分块 ID、版本、租户、ACL、有效时间、内容哈希和删除标记；支持增量更新、幂等、断点续传、失败重试、索引切换和回滚。

  4. 安全检索、重排、上下文与引用
     实现检索前的强制权限过滤、可调混合召回、分数保留、去重、父子块扩展、上下文 Token 预算以及精确到文档版本的引用。

  5. 有界 LangGraph 与持久化会话
     完善状态模型，保留原始问题和改写历史；增加步骤、检索、生成、Token 和截止时间预算；加入无进展检测、原因码、持久化 checkpoint、TTL 和会话摘要。

  6. RAG Service API 与外部契约
     提供版本化查询、流式输出、会话、健康检查和就绪检查 API；传递租户/用户身份；定义 Dify、IAM、ONECRM、审批系统的接口契约，但不把这些外部系统实现在 RAG 内部。

  7. 可观测性、稳定性、性能与降级
     完善 Langfuse/链路追踪、指标脱敏、模型和索引版本记录；增加超时、重试、熔断、限流、并发控制、降级矩阵、压测和故障注入。

  8. 部署、安全加固与证据包
     补充容器、环境配置、密钥管理、非 root 运行、镜像锁定、漏洞扫描、迁移和回滚手册，并让每项企业能力都有测试或报告支撑。

  推荐顺序是：1+2 → 3 → 4 → 5 → 6 → 7 → 8。评测模块贯穿整个实施过程。模拟文档中的 TopK、分块大小、向量维度、SLO 等数值不会直接照搬，必须通过真实评测和压测确定。

  完整分析和验收标准已记录在 Desktop/workspace/RAG企业知识库项目/RAG_PROJECT/.planning/2026-09-04-rag/findings.md，执行计划位于 Desktop/workspace/RAG企业知识库项目/RAG_PROJECT/.planning/2026-09-04-rag/
  task_plan.md。


  ---

  八个实施模块已全部完成，项目目前具备可验证的企业级 RAG 工程基线，而不再只是模
  拟文档描述。

  主要交付：

  - 企业能力与证据映射：`docs/企业级能力证据索引/0.企业级能力证据索引.md`

  - 生产部署、迁移和回滚流程：`docs/部署与回滚手册.md`

  - 事故响应手册：`docs/应急响应手册.md`

  - 非 root、摘要固定、CPU-only 镜像：Desktop/workspace/RAG企业知识库项目/
    RAG_PROJECT/Dockerfile:1

  - 只读文件系统、密钥必填、模型只读挂载：Desktop/workspace/RAG企业知识库项目/
    RAG_PROJECT/deploy/docker-compose.yml:1

  - 生产启动安全校验：Desktop/workspace/RAG企业知识库项目/RAG_PROJECT/
    rag_service/settings.py:276

  - CI 已加入依赖审计、SBOM、镜像漏洞和密钥扫描：Desktop/workspace/RAG企业知识库
    项目/RAG_PROJECT/.github/workflows/ci.yml:1

  验证结果：

  - 94 项离线测试全部通过。
  - Python 编译、YAML 解析、pip check、Git 空白检查全部通过。
  - ARM64 Linux 镜像实际构建成功，容器 UID 为 10001。
  - 镜像使用官方 PyTorch CPU wheel
    (https://download.pytorch.org/whl/cpu/torch/)，没有引入 CUDA 运行时。

  - 本地服务已启动：http://127.0.0.1:8011
  - Liveness、readiness、OpenAPI 均返回 HTTP 200；未认证查询正确返回 HTTP 401。

  目前仍需真实环境完成的工作包括 Milvus 数据迁移与索引回滚演练、IAM/LLM/Dify 联
  调、生产负载测试和 SLO 校准，以及多副本部署前替换 SQLite 为共享检查点存储。这
  些限制已经明确记录，没有使用模拟文档中的数字冒充生产结果。用户原有修改、删除文
  件和 16 份模拟文档均未被覆盖。

---

## 2026-09-04 共享状态与多副本补齐

上一轮结论中的“多副本部署前替换 SQLite”现已完成工程实现：

- SQLite 保留为本地单进程模式；生产启动强制使用 PostgreSQL + Redis。
- PostgreSQL 统一保存 LangGraph checkpoint、pending writes、会话归属、摘要与 TTL。
- Redis 提供跨 Pod 会话 lease、原子租户令牌桶与定时清理选主；故障时 fail closed。
- 提供 SQLite 只读盘点、短维护窗口迁移、源指纹审计、语义核对和幂等重跑工具。
- Kubernetes 基线为 3 副本，包含跨区调度、PDB、HPA、探针、资源边界、Schema Job 与清理 CronJob。
- 新增真实 PostgreSQL/Redis 隔离集成测试及共享状态负载工具。

这代表“共享状态多副本工程基线和本地真实依赖测试”已补齐，不代表已在真实生产集群完成 HA、灾备、浸泡、容量与 SLO 验证。剩余阻塞项以 `docs/企业级能力证据索引/0.企业级能力证据索引.md` 为准。
